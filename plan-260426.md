## 数据集
已经将每个数据集按训练集和测试集分别转换为 csv 文件，且均包含 text 和 label 列。

```
datasets/
├── emotion
│   ├── emotion_test.csv
│   └── emotion_train.csv
├── gigaword
│   ├── gigaword_test.csv
│   └── gigaword_train.csv
├── sst5
│   ├── sst5_test.csv
│   └── sst5_train.csv
├── sts12
│   ├── sts12_test.csv
│   └── sts12_train.csv
├── stsb
│   ├── stsb_test.csv
│   └── stsb_train.csv
├── wmt19_En-Zh
│   ├── wmt19_En-Zh_test.csv
│   └── wmt19_En-Zh_train.csv
└── wmt19_Zh-En
    ├── wmt19_Zh-En_test.csv
    └── wmt19_Zh-En_train.csv
```

`datasets/emotion/emotion_test.csv`示例：
```csv
text,label
im feeling rather rotten so im not very ambitious right now,0
i left with my bouquet of red and yellow tulips under my arm feeling slightly more optimistic than when i arrived,1
```

## 下一步任务：代码迁移

1. 把 CoT 这种 baseline 形式 迁移到新整理的 datasets/*/{train,test}.csv（统一 text,label）上。
2. 结果文件需要和 similarity/sts12/glm4_9b/result/*.csv 的主字段风格对齐，包含：Text, truth, pre, mse, rmse, pearson_coeff, spearman_coeff, input_tokens, output_tokens, total_tokens, runtime，并且末尾追加 metric,value 的汇总行。
3. 现有 similarity/sts12/glm4_9b/*.py 能看出两个强约束：
  - 推理方式：transformers + AutoTokenizer.apply_chat_template + batched generate
  - 输出规范：每行预测 + 全局指标重复列 + token/runtime 统计 + 文件尾汇总
4. `similarity/sts12/cifar/cot.py` 当前是图像脚本（CIFAR），里面“cot”更多是提示词风格，不是可直接用于文本 CSV 的实现。所以应该抽象成“通用文本 CSV runner + baseline 策略层”，而不是硬改死某一个数据集/某个模型。
5. 后续很可能还要迁移 random/fixed/sc-cot/sv-cot/few-shot-cot 等 baseline，所以这次应该先分析清楚哪些代码是通用的/要做成配置文件（如果要配置，用`py`或`yaml`），哪些是任务/数据集/模型特定的，然后把底座搭好：
  - 通用部分：数据加载、模型加载、批推理、输出格式、指标/计时/token 统计
  - 配置部分：模型路径、数据集路径、任务类型（分类/回归/生成）、提示词模板
  - 策略部分：先实现 cot，后续在同一框架里继续加其他 baseline。
6. 优先跑下面的模型：
  * GLM-4-9B：/mnt/huawei/ymb/model/glm-4-9b/model
  * LLaMA3.1-8B：/mnt/huawei/ymb/model/Llama-3.1-8B/model
  
请你先理解项目背景、任务要求，然后开始实现代码。